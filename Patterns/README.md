#				Patterns

#### —одержание: ####

[Singleton](https://...)

[...](https://...)

_________________________________________________________________________________________

#				Singleton
```
 ласс, должен иметь только один экземпл€р, при этом необходимо обеспечить:
* глобальную точку доступа к экземпл€ру
* потокобезопасность
```
ѕредставлен сравнительно простой моделью с различными вариантами реализации.
_________________________________________________________________________________________

#				—тратегии реализации

#### Singleton ####

![screen capture 1](01.png)

Ёта реализаци€ имеет два основных преимущества:
1. ѕоскольку экземпл€р создаетс€ внутри метода свойства экземпл€ра, класс может выполн€ть
дополнительные функции (например, создавать экземпл€р подкласса), даже если это может
привести к нежелательным зависимост€м.

2. —оздание экземпл€ра не выполн€етс€ до тех пор, пока объект не запросит экземпл€р;
этот подход называетс€ отложенным созданием экземпл€ра или "ленивым экземпл€ром".

ќтложенное создание экземпл€ров позвол€ет избежать создани€ ненужных синглетов при
запуске приложени€.

ќсновным недостатком этой реализации €вл€етс€ то, что она небезопасна дл€ многопоточных
сред. ≈сли отдельные потоки выполнени€ выполн€т метод свойства экземпл€ра одновременно,
может быть создано несколько экземпл€ров Singleton.
 аждый поток может выполнить следующую инструкцию решив, что нужно создать новый
экземпл€р:
```
if (instance == null)
```

#### Static Initialization ####
